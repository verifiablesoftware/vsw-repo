{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node"
        },
        {
            "command": "${workspaceFolder}/app/repo-start-local-debug.sh",
            "name": "Run vsw-repo locally start",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}/app"
        }
    ]
}